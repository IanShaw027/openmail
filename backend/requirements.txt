# ── Runtime ────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sqlalchemy>=2.0.36
pydantic>=2.10.0
pydantic-settings>=2.6.0
cryptography>=43.0.0
python-multipart>=0.0.12
httpx>=0.27.0
# SOCKS5 proxies (socks5://warp-N:1080) for httpx
socksio>=1.0.0
# TLS fingerprint for mail.com cookie provider; code falls back to httpx if absent
curl_cffi>=0.15.0

# ── Test-only (local pytest / CI; the production image does not use this file) ───
# Production installs from pyproject.toml + uv.lock (see Dockerfile).
# Keep runtime deps above this header so `pip install -r requirements.txt`
# still matches what tests import.
pytest>=8.3.0
pytest-asyncio>=0.24.0
# Starlette TestClient (FastAPI tests) — required since starlette 1.3 deprecation
httpx2>=2.0.0
